{
	"name": "we",
	"scripts": {
		"editor:build": "bun run --filter ./packages/we-editor build",
		"playground:dev": "bun run --filter ./packages/we-playground dev",
		"playground:build": "bun run --filter './packages/playground' build",
		"format": "biome format --write",
		"lint": "biome lint"
	},
	"workspaces": [
		"packages/*"
	]
}
